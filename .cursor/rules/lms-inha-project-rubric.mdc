---
description: Inha LMS group project — rubric R1–R13 and deliverables status (read PROJECT_REQUIREMENTS_STATUS.md)
alwaysApply: true
---

# LMS project (Inha DB course)

When planning features, grading gaps, or pre-submission checks for this repo:

1. Read **`PROJECT_REQUIREMENTS_STATUS.md`** at the repository root for the full **R1–R13** table, **deliverables** (URL, GitHub, PDF), **LMS scenario gaps**, and **priority list**.
2. **Not implemented in code yet:** **R12** (unified traces/logs/metrics); **R8** TLS on Traefik for production; **R6** quantitative write-up is report work; **R1/R2** are report-first.
3. **Forbidden / viva:** Hand-drawn ER (not ORM-only artefact); BPMN for **every** pipeline workflow in the PDF; team can explain backend line-by-line; polyglot must be real (Postgres + Redis OK).

Keep `PROJECT_REQUIREMENTS_STATUS.md` updated as the system changes.
